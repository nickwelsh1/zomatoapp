import React from 'react';

const Card = ({ restaurant, id }) => { //destructuring to create simple properties
    return (
        <div >
            <h2>card</h2>
        </div>
    );
}

export default Card;